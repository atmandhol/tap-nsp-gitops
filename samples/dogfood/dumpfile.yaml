#@ load("@ytt:data", "data")
#@ load("@ytt:json", "json")
---
apiVersion: v1
kind: Secret
metadata:
  name: vault-dump
type: kubernetes.io/basic-auth
stringData:
  values: #@ json.encode(data.values.imported)
